# Configuração para usar persistence local (In-Memory) ao invés de Cassandra
# OTIMIZADA: Minimiza snapshots para melhor performance

include "application.conf"

pekko {
    persistence {
      journal {
        plugin = "pekko.persistence.journal.inmem"
        auto-start-journals = ["pekko.persistence.journal.inmem"]
      }
      snapshot-store {
        plugin = "pekko.persistence.snapshot-store.local"
        auto-start-snapshot-stores = ["pekko.persistence.snapshot-store.local"]
      }
    }
}

# Configuração do In-Memory Journal (sem dependências externas)
pekko.persistence.journal.inmem {
  # In-memory journal - não precisa configuração especial
}

# Configuração do Local Snapshot Store
pekko.persistence.snapshot-store.local {
    dir = "/app/hyperbolic-time-chamber/snapshots"
}

# 🆕 OTIMIZAÇÕES DE PERFORMANCE PARA 1M SIMULAÇÕES
pekko.actor {
    default-dispatcher {
        type = Dispatcher
        executor = "fork-join-executor"
        fork-join-executor {
            parallelism-min = 16
            parallelism-factor = 2.0
            parallelism-max = 64
        }
        throughput = 100
    }
}

# 🛑 REDUZIR SNAPSHOTS PARA PERFORMANCE
pekko.persistence {
    # Snapshots menos frequentes para simulações grandes
    snapshot-store.local.snapshot-interval = 10000  # Snapshot a cada 10k eventos (padrão: 1000)
    
    # Configurações para reduzir overhead
    journal.inmem {
        # Configurações para reduzir memory usage
        max-message-batch-size = 200
    }
}